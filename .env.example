# Base URL of the Django backend (must match where `python manage.py runserver` listens).
# After creating .env, restart `npm run dev` so Next picks this up.
# In development, http://127.0.0.1:8000 is used if this line is omitted.
NEXT_PUBLIC_DJANGO_API_URL=http://127.0.0.1:8000

